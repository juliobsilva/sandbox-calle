name: API Mgmt APIOPS Publisher

on:
  pull_request:
    branches: [main, develop]
    types: closed

  workflow_dispatch:
    inputs:
      destination-enviroment:
        type: string
        required: true
        default: hml
      commit-id:
        type: string
        required: false

jobs:

  call-build-pr:
    uses:  juliobsilva/api-management/.github/workflows/apim-apiops-publisher.yaml@main
    with:
      destination-enviroment: ${{ github.event_name == 'pull_request' && 'hml' || inputs.destination-enviroment }}
      commit-id: ${{ inputs.commit-id }}
    secrets: inherit